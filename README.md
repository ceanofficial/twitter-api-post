# X JSON Poster

A simple Node.js application that posts content from a local JSON file directly to X (formerly Twitter) using the official X API.

## ✨ Features

- Reads posts from a local `posts.json` file
- Posts tweets automatically using the X API v2
- Secure credential handling via environment variables
- Clean, minimal, and easy to extend
- Suitable for bots, schedulers, or CI automation

---

## 📦 Requirements

- Node.js v18+
- An X Developer Account
- X App with **Read and Write** permissions enabled

---

## 🔑 Setup

### 1. Clone the repository

```bash
git clone https://github.com/your-username/x-json-poster.git
cd x-json-poster
